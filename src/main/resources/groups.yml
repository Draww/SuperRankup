# Only the custom permission provider will be used.
# prefixes will be used by any chat format plugin. (plugins like EssentialsChat)
# Place %superrup_group% in the format settings of the chat format plugin config file.

Example_Rank:
  prefix: "&a[Example]"